apiVersion: v2
name: bookinfo-processor
version: 1.0.4-SNAPSHOT
appVersion: 1.0.4-SNAPSHOT
type: application
description: A Helm chart for Bookinfo Processor microservice
sources:
  - https://github.com/muralikrishnagk/bookinfo-source
maintainers:
  - name: Akhil Cheruvalath
    email: your.email@example.com

# Default values for bookinfo
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Values from this file will be used as default values in the Helm templates
# These can be overridden by values in the kustomization files or via ArgoCD
